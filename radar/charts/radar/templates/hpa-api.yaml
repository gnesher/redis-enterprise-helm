{{- if .Values.app.autoscaling.enabled -}}
{{- $cpuTarget := .Values.app.autoscaling.targetCPUUtilizationPercentage -}}
{{- $memoryTarget := .Values.app.autoscaling.targetMemoryUtilizationPercentage -}}
{{- if and (eq $cpuTarget nil) (eq $memoryTarget nil) -}}
{{- fail "app.autoscaling requires targetCPUUtilizationPercentage or targetMemoryUtilizationPercentage when enabled" }}
{{- end }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "radar.fullname" . }}-api
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "radar.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "radar.fullname" . }}-api
  minReplicas: {{ .Values.app.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.app.autoscaling.maxReplicas }}
  metrics:
    {{- if ne $cpuTarget nil }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ $cpuTarget }}
    {{- end }}
    {{- if ne $memoryTarget nil }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ $memoryTarget }}
    {{- end }}
{{- end }}
